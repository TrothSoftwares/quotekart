  <% if dealer_signed_in? %>
                <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        <span class="glyphicon glyphicon-user"></span> 
                        <strong><%= current_dealer.email %></strong>
                        <span class="glyphicon glyphicon-chevron-down"></span>
                    </a>
                    <ul class="dropdown-menu">
                        <li>
                            <div class="navbar-login">
                                <div class="row">
                                    <div class="col-lg-4">
                                        <p class="text-center">
                                            <span class="glyphicon glyphicon-user icon-size"></span>
                                        </p>
                                    </div>
                                    <div class="col-lg-8">
                                        <p class="text-left"><strong><%= current_dealer.shopname %></strong></p>
                                        <p class="text-left small"><%= current_dealer.email %></p>
                                        <p class="text-left">
                                            <%= link_to 'Edit profile', edit_dealer_registration_path, :class => 'btn btn-primary btn-block btn-sm' %> 
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </li>
                        <li class="divider"></li>
                        <li>
                            <div class="navbar-login navbar-login-session">
                                <div class="row">
                                    <div class="col-lg-12">
                                        <p>
                                            <%= link_to "Logout", destroy_dealer_session_path, method: :delete, :class => ' btn btn-danger btn-block'  %>
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </li>
                    </ul>
                </li>
            </ul>
            
            
  <% else %>
  
 <%= form_for(:dealer, :url => session_path(:dealer) , :html => {:class => 'navbar-form navbar-right', 'role' => 'form'}) do |f| %>
 
 <div class="input-group">
 <span class="input-group-addon"><i class="glyphicon glyphicon-user"></i></span>
  <%= f.text_field :email , class: "form-control" , placeholder: "Email Address" %>
  </div>
 <div class="input-group"> 
	<span class="input-group-addon"><i class="glyphicon glyphicon-lock"></i></span>
  <%= f.password_field :password ,  class: "form-control" , placeholder: "Password" %>
  </div>
  <div class="input-group">
  <%= f.submit 'Sign in', class: "btn btn-primary" %>
  </div>
<% end %>
<% end %>